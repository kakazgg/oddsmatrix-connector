/// <reference types="node" />
import { HttpConnectionState } from './http.utils';
export default class HTTPConnector {
    host: string;
    port: string;
    nextScheduledRequestId: NodeJS.Timeout;
    updateInterval: number;
    connectionState: HttpConnectionState;
    constructor(host: string, port: string);
    setState(newState: Partial<HttpConnectionState>): void;
    getQueryParams(): string;
    nextRequest(): Promise<void>;
    connect(subscriptionSpecificationName: string, updateInterval: number): Promise<void>;
    disconnect(): Promise<void>;
}
