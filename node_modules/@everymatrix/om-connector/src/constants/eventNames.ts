export enum TCPEvents {
    jsonReceived = 'jsonReceivedTcp',
    jsonUnzipped = 'jsonUnzippedTcp',
    notifyInitialDump = 'notifyInitialDumpTcp',
    notifyEntityUpdates = 'notifyEntityUpdatesTcp',
    ping = 'pingTcp',
    tcpError = 'errorTcp',
    subscribe = 'subscribeTcp',
    unsubscribe = 'unsubscribeTcp',
}

export enum HTTPEvents {
    jsonReceived = 'jsonReceivedHttp',
    jsonUnzipped = 'jsonUnzippedHttp',
    notifyInitialDump = 'notifyInitialDumpHttp',
    notifyEntityUpdates = 'notifyEntityUpdatesHttp',
    nextRequest = 'makeNextRequestHttp',
    reconnect = 'reconnectHttp',
    httpError = 'httpError'
}

export enum CommonEvents {
    runtimeError = 'runtimeError',
}
